<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.predator.hi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminbookingclassActivity"></activity>
        <activity android:name=".AdminbookingservicesActivity" />
        <activity android:name=".Checkbooking" />
        <activity android:name=".MyservicesActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MybookingActivity" />
        <activity android:name=".HomeservicesActivity" />
        <activity android:name=".MengajiActivity" />
        <activity android:name=".BookingActivity" /> <!-- Start of Splash Screen Activity -->
        <activity
            android:name=".LoadingScreen"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- End of Splash Screen Activity -->
        <!-- Start of Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- End of Login Activity -->
        <!-- Start of Main Activity -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- End of Main Activity -->
        <!-- Start of Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>